# React-Chat
Simple client-server chat implemented with `Node.js`, `React` and `Socket.io`.

## To start the app 
* first execute `npm i` and `npm run build` within the ui folder.
* then execute `npm i` and `npm start` from the root of project. 
* Server will start on `localhost:4000` by default.